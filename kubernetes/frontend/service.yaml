apiVersion: v1
kind: Service
metadata:
  name: expense-app-frontend-service
spec:
  selector:
    app: expense-app-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer